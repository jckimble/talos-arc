apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: prod-infra
  namespace: flux-system
spec:
  dependsOn:
    - name: prod-platform
  interval: 10m
  path: ./clusters/prod/infra
  prune: true
  wait: true
  timeout: 10m
  retryInterval: 1m
  sourceRef:
    kind: GitRepository
    name: platform-repo
  decryption:
    provider: sops
    secretRef:
      name: sops-age
